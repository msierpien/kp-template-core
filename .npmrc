# Publikacja idzie do GitHub Packages (rejestr per scope).
# Token (write:packages) NIE nalezy do repozytorium - ustaw go w srodowisku:
#   export NODE_AUTH_TOKEN=ghp_...
@msierpien:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${NODE_AUTH_TOKEN}
